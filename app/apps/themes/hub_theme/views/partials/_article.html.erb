<div class="content">
  <h1><%= @post.the_title %></h1>
</div>
<div class="article-created-at">
  <i class="fa fa-calendar"></i> <%= @post.created_at.strftime("%B %d, %Y") %>
</div>
<div class="">
  <%= @post.the_content %>
</div>
